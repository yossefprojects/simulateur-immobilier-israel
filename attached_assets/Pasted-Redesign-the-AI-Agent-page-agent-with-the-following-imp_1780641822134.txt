Redesign the AI Agent page (/agent) with the following improvements. Keep all existing logic, routing, state management, and API calls intact — only update the UI/CSS.

---

## HEADER / NAV (top bar)

- Keep the "Israel Real Estate Simulator" branding and subtitle
- Keep the NadlanConnect button, EN language toggle, and 3-dot menu
- Nav tabs (Valuation, Urban potential, Investor analysis, Developer P&L, Taxation, Renovation costs, AI Agent):
  - Inactive tabs: font-size 13px, muted color, no border
  - Active tab "AI Agent": gold background (#C9A84C), color #0D1B3E, font-weight 500, border-radius 6px, padding 6px 14px, with the ✦ icon kept

---

## HERO BANNER (the dark AI visual strip)

Remove the decorative dark banner with the "AI" circle and "SCORE 78/100" completely.
Replace it with a clean compact header section integrated directly above the main card:
- Background: #0D1B3E, padding 20px 32px
- Left: title "Shamai AI Agent — Property Valuation" at 18px font-weight 500 white, with the robot emoji icon kept
- Right: "History 8" button (keep existing behavior) + "Claude · Replit AI" badge — style as small pills with 0.5px border rgba(255,255,255,0.2), font-size 12px, white at 70% opacity
- Below title: subtitle "Licensed Israeli appraisal expert (indicative) — market value, taxation and urban potential." at 13px, white at 50% opacity

---

## MAIN CARD

- Background: white, border 0.5px solid rgba(0,0,0,0.08), border-radius 12px, padding 24px
- No outer shadow
- Page background: #F7F5F0 (warm off-white), padding 24px 32px

---

## EXAMPLE PROPERTIES chips

- Label "EXAMPLE PROPERTIES": uppercase, font-size 11px, letter-spacing 0.08em, muted color, margin-bottom 8px
- Chips: border 0.5px solid rgba(0,0,0,0.12), border-radius 20px, padding 5px 14px, font-size 12px, background white, color dark
- Hover: background #FBF5E6, border-color #C9A84C, color #0D1B3E
- Keep all existing onClick behavior

---

## QUICK FIELDS accordion

- Header row: full width, flex between label "QUICK FIELDS (OPTIONAL)" (uppercase 12px, font-weight 500, muted) and chevron icon
- Border: 0.5px solid var(--color-border-secondary), border-radius 8px, padding 12px 16px
- When expanded, show fields in a 2-column grid (gap 10px) inside the same bordered container:
  - Each field: label 11px uppercase spaced above, input with light gray background (#F7F5F0), border 0.5px, border-radius 8px, padding 8px 10px, font-size 13px
  - On focus: border color #C9A84C, background white
- Fields to include: City / Neighborhood, Property type, Surface (m²), Floor, Year built, Condition, Objective (full width)

---

## MAIN TEXTAREA

- Replaces the current large input area
- min-height: 140px, padding 14px 16px
- Border: 0.5px solid rgba(0,0,0,0.12), border-radius 10px
- Background: white
- Font-size 14px, line-height 1.6, color dark
- Placeholder: keep existing text
- On focus: border color #C9A84C
- No resize handle visible (resize: vertical is fine but style it cleanly)

---

## BOTTOM ACTION ROW

- Flex row, space-between, align-items center
- Left: "Clear" button with ti-trash icon, font-size 13px, muted color, transparent background, no border. Hover: color darkens.
- Center: hint text "Ctrl+Enter" in a small muted badge (font-size 11px, background #F0EDE6, border-radius 4px, padding 2px 6px)
- Right: "Evaluate property" button — background #C9A84C, color #0D1B3E, font-weight 500, border-radius 8px, padding 10px 22px, font-size 14px, with the send/arrow icon kept. Hover: #b8963e.

---

## DISCLAIMER

- Keep: "Indicative estimate — Israeli market 2025. Does not replace a legally signed Shamai (שמאי) report."
- Style: font-size 12px, color muted, text-align center, margin-top 16px
- No background, no border

---

## GENERAL RULES

- Remove all decorative dark backgrounds, radar/circle animations, and the SCORE badge from the hero
- No gradients, no drop shadows, no blur
- All colors must work in both light and dark mode — use CSS variables where applicable
- Do not change any route, API call, state, or business logic
- Do not change any existing string content or translations
- Preserve all existing IDs and JS-referenced class names
- Mobile (< 768px): quick fields grid collapses to 1 column, action row stacks vertically with button full width